[package]
name = "civ-engine"
version = "0.1.0"
edition = "2021"
description = "Staging crate for the Rust game engine domain"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
