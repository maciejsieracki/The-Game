# B3-Q1 + B4 — SVG w menu pauzy i panelu badań

**B3:** A — ikony SVG w `gamePauseMenu.ts` (w tym „Wróć do gry”).

**B4-Q1:** A — klik w węzeł drzewka ustawia cel, drzewko pozostaje otwarte (`onSelectTarget`).

**B4-Q2:** A — przycisk „Pełne drzewko” pod aktywnym celem (nagłówek panelu Badania).

**B4-Q3:** B — emoji zastąpione SVG z brand pack (`res-science`, `ui-lock`, `ui-close`, `chip-map`).

**Status:** ✅ WDROŻONE
