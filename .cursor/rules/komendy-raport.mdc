---
description: Stałe komendy właściciela i minimalny format raportu AutoBot.
alwaysApply: true
---

# Komendy i raport — reguła techniczna

Komenda `raport` obejmuje aktywną kolejkę, dowody etapów i następne bramki; nie dopisuj
tematów na podstawie samego statusu. Komenda `sprawdź` audytuje całą bieżącą pulę oraz
historyczne `not_found` do reconciliacji. Komendy `pytania`, `status` i `co dalej`
odpowiadają wyłącznie na podstawie plikowych dowodów.

Raport roli ma pola: `STATUS`, `TEMAT`, `GOAL`, `ZMIANY/COMMIT`, `TESTY`,
`BLOKADY`, `NASTĘPNY KROK`, `DEPLOY/PUSH`. Brak raportu lub werdyktu
Evaluator/Final Control oznacza brak dowodu zakończenia. Pełny routing i hierarchia:
[`README.md`](../../README.md) i [`INDEX-PROCESU.md`](../../docs/procesy/INDEX-PROCESU.md).

## Kontrola kompletności zgłoszeń (C-031)

Po każdej serii rejestracji w `dyspozycje/PYTANIA-OTWARTE.md`, przed zmianą
wątku — uruchom `grep -nE 'STATUS:[[:space:]]*\*{0,2}OTWARTE' dyspozycje/PYTANIA-OTWARTE.md`
(komenda poprawiona 2026-09-05 — stara forma `grep -n 'STATUS: \*\*OTWARTE'` widziała
31 z 71 realnych trafień, bo gubiła formy `**STATUS: OTWARTE …**` i nagłówkowe;
patrz `P-AUDYT-STATUS-OTWARTE-REGEX-SLEPOTA` i CLAUDE.md §0c)
(bez kotwicy `^## ` — gubi nagłówki `### `) i potwierdź dla każdego trafienia:
subagent w locie / pytanie ABC zadane / udokumentowany powód odłożenia. Ta sama
komenda żyje TAKŻE w `CLAUDE.md` §0c — dwa niezależne, zawsze ładowane nośniki
dla dwóch różnych narzędzi.
