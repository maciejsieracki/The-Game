# R-WOJNA-WYMUSZONA-PAROWANIE-ZAMIAST-DOMINA-Q1 — Evaluator, runda 1/5

**Uwaga proceduralna:** agent Evaluator zwrócił kompletną treść tego raportu przez
StructuredOutput, ale proces zakończył się przed zapisaniem/zacommitowaniem pliku na dysku.
Orkiestrator odtwarza ten raport z zarejestrowanej treści zwrotnej agenta, bez zmiany
choćby jednego znaku, i commituje w jego imieniu.

STATUS: FAIL
DOMAIN: GAME
TEMAT: R-WOJNA-WYMUSZONA-PAROWANIE-ZAMIAST-DOMINA-Q1
GOAL: Jedna wspólna funkcja `assignForcedWarPairings` zastępująca coordinated-pick i domino trójstronne — krok 1 ECHO: podmioty bez wojny parowane 1v1 pierwsi; krok 2: nieparzysta reszta dołącza jako trzeci; gracz jak AI; brzegowy przypadek → `unresolvedOwnerIds`/DECISION_REQUIRED.
ZMIANY-COMMIT: Brak commita (worktree `wt-wojny-domino`, baza `8b3ddfaa`, zgodna z `00-dispatch.md`). `git status` zweryfikowany niezależnie: zgadza się 1:1 z raportem Operatora — `forced-war-{common,bronze,stone,iron}.ts`, `main.ts`, 8 istniejących bramek, nowa `wojna-wymuszona-parowanie-test.cjs`, nowy `01-operator.md`. `gra/src/game/ai.ts` — **zero zmian, potwierdzone** (`git diff --stat` puste). `society-breakdown.ts`, `order.ts`, `docs/decyzje/**`, `WERSJE.md`, `playbook.json`, `gra-robocza/**` — zero zmian, potwierdzone.

TESTY (wszystkie uruchomione samodzielnie, z `gra/`):
- `npx tsc --noEmit` → 0 błędów (potwierdzone).
- 5 referencyjnych: 213/213, 19/19, 33/33, 13/13, 6/6 — wszystkie zielone, potwierdzone niezależnie.
- Cała rodzina (16 plików `find gra/tools -iname "*forced-war*" -o -iname "*wojna*wymuszona*"`, uruchomione KAŻDY, nie tylko wymienione w raporcie): bronze-test 56/56, bronze-main-guard 28/28, bronze-new-game-reset 34/34, stone-test 38/38, stone-main-guard 19/19, iron-test 55/55, iron-main-guard 37/37, iron-era-enter-turn-save-load 20/20, trojstronna-test 23/23, trojstronna-main-guard 14/14, reguly-multi-turn-simulation 39/39, **p-wojna-wymuszona-trzy-naprawy-test 13/13 (bonus, nie w liście raportu — też zielony)**, nowa wojna-wymuszona-parowanie-test 40/40 — **wszystkie zgadzają się z raportem, żadna liczba nie jest zawyżona**.
- 2 bramki Playwright (real Chromium, real vite build), NIE wymienione w raporcie jako uruchomione w tej rundzie: `forced-war-player-target-live-test.cjs` **11/11 PASS** i `forced-war-iron-player-target-live-test.cjs` **11/11 PASS** — potwierdzone niezależnie w żywej przeglądarce.
- `forced-war-trojstronna-domino-live-test.cjs` — uruchomiony samodzielnie (Playwright): **22 pass, 2 fail**, dokładnie z przyczyny zgłoszonej w BLOKADACH (nowy algorytm daje cel tylko JEDNEJ stronie domina, stary test oczekuje OBU) — potwierdzone, nie zawyżone ani nie ukryte.
- `forced-war-iron-mutant-probe.cjs` — uruchomiony samodzielnie: FAIL, dokładnie z przyczyn zgłoszonych (M09/M10 pre-istniejące + M38-M44/M56 celujące w usunięty kod per-owner) — potwierdzone.
- Własne 15 scenariuszy (import `assignForcedWarPairings` bezpośrednio przez esbuild, zero reużycia kodu testu Operatora): odtworzenie incydentu Rzymu (sojusznik gracza dołącza jako trzeci do niepowiązanej pary → dostaje realną wojnę, NIE zero), literalna wersja incydentu (jedyna istniejąca para ma gracza jako stronę, sojusz Rzym↔gracz blokuje CAŁĄ parę → Rzym poprawnie ląduje w `unresolvedOwnerIds`, nie cicho z zerem), gracz sparowany 1v1 z AI symetrycznie jak każde AI, nieparzysta liczba bez żadnej pary do dołączenia → poprawnie `unresolvedOwnerIds` (DECISION_REQUIRED, nie zgadywanie) — 14/15 PASS, patrz BLOKADY niżej dla 1 FAIL istotnego.

BLOKADY:
1. Potwierdzone, PRE-ISTNIEJĄCE (zgodne z raportem Operatora, nie nowe): `forced-war-iron-mutant-probe.cjs` (FAIL) i `forced-war-trojstronna-domino-live-test.cjs` (22/2 fail) — Operator zgłosił je uczciwie, moje niezależne uruchomienia dają ten sam obraz.
2. **NOWE, znalezione własną konstrukcją adwersaryjną (patrz ZARZUT 2)**: rdzeń `assignForcedWarPairings` (krok 3, parowanie 1v1) może NIE znaleźć istniejącego poprawnego parowania pełnego, gdy blokady (sojusz/NAP/cooldown tej samej pary) tworzą zapętloną strukturę — reprodukowalne, patrz niżej.

RUNDY: 1/5
NASTĘPNY KROK: Obrona Operatora per zarzut (§3c) — zarzut 2 wymaga poprawki algorytmu (nie tylko dopisania testów), zanim runda 2 zajmie się dwoma już zgłoszonymi blokadami.
DEPLOY-PUSH: NIE WYKONANO

ZARZUTY:

**1. [Miejsce: `gra/src/main.ts`, allowlista `00-dispatch.md` §ALLOWLISTA]** — Literalna allowlista mówi wprost: „`gra/src/main.ts` (WYŁĄCZNIE punkt wywołania przed `ownerLoop`, ok. linii 30360-30408, oraz debug-hook … — **zakaz zmian gdziekolwiek indziej w main.ts**)". Faktyczny `git diff` pokazuje edycje/usunięcia sięgające ok. linii 30900, 31136-31257 i 31264-31444 — głęboko WEWNĄTRZ `ownerLoop` (usunięcie trzech całych per-owner bloków coordinated-pick+domino dla Brązu/Kamienia/Żelaza). Operator sam to ujawnił w raporcie jako „interpretacja do ratyfikacji", ale **wykonał zmianę zanim ratyfikacja nastąpiła**, zamiast zatrzymać się i zgłosić `DECISION_REQUIRED` (C-054) przed edycją. Łagodząca okoliczność: sekcja „ROZSTRZYGNIĘCIE ZAKRESU" tego samego dispatchu (autorstwa orkiestratora) explicite wymaga zastąpienia OBU mechanizmów (coordinated-pick I domino) jedną procedurą — co jest fizycznie niemożliwe bez dotknięcia bloków wewnątrz `ownerLoop`, gdzie coordinated-pick żył. Dispatch zawiera więc wewnętrzną sprzeczność (GOAL/ROZSTRZYGNIĘCIE ZAKRESU vs. literalna ALLOWLISTA) — to musi być jawnie rozstrzygnięte (ODDAL z uzasadnieniem albo DO DECYZJI CZŁOWIEKA), nie przemilczane.

**2. [Miejsce: `gra/src/game/forced-war-common.ts`, funkcja `assignForcedWarPairings`, krok 3 (pętla `while (remaining.length > 0)`, linie ok. 254-272)]** — Algorytm parowania 1v1 to zachłanny „najbliższy nieblokowany kandydat, przetwarzany w kolejności ownerId", NIE pełne dopasowanie (matching) grafu. Własna konstrukcja adwersaryjna, uruchomiona bezpośrednio na realnej funkcji (esbuild, zero reimplementacji): 4 podmioty warless {1,2,3,4}, zablokowane pary {1-4, 2-3, 3-4} (dowolna kombinacja NAP/sojusz/cooldown tej samej pary — `isPeaceLockedBetween` jest DOKŁADNIE tym mechanizmem, który naturalnie akumuluje się po każdej zakończonej wojnie wymuszonej, więc taka gęsta topologia blokad jest realistyczna w długiej rozgrywce, nie tylko teoretyczna). Istnieje poprawne pełne parowanie {1-3, 2-4} (obie pary odblokowane) — ale algorytm zachłannie paruje 1-2 jako pierwsze (najniższe ownerId, remis dystansu), zostawiając 3 i 4 OBU bez pary; przy braku istniejącej pary do dołączenia jako trzeci obaj lądują w `unresolvedOwnerIds` (DECISION_REQUIRED), mimo że rozwiązanie realizujące ECHO („najpierw wszyscy mają wojnę") istniało i było osiągalne. Nie jest to naruszenie WĄSKIEGO binarnego kryterium w jego dosłownym brzmieniu (nikt nie ma ≥2 w tym konkretnym przykładzie), ale bezpośrednio przeczy zamierzeniu ECHO i może w praktyce wywoływać fałszywe `DECISION_REQUIRED` dla cywilizacji, które dawałoby się normalnie sparować. Żaden z 9 scenariuszy nowej bramki (`wojna-wymuszona-parowanie-test.cjs`) ani żaden scenariusz `forced-war-reguly-multi-turn-simulation-test.cjs` nie konstruuje takiej zapętlonej topologii blokad (scenariusze 3/8 testują wyłącznie POJEDYNCZĄ blokadę, nie dwie/trzy przecinające się) — stąd „40/40 ALL GREEN" nie wykrywa tej luki. Reprodukcja: `node` na zbudowanym bundlu z `assignForcedWarPairings([{1,2,3,4} q=0,r=0,era:'bronze'], [], {isPairBlocked: blokuj 1-4,2-3,3-4, ...})` → `unresolvedOwnerIds: [3,4]`.
