STATUS: FAIL
DOMAIN: GAME
TEMAT: R-HOTSEAT-ETAP6D-PODETAP-D-Q1
GOAL: Migracja duplikatu tick dyplomacji wewnątrz `runWorldEndTurn` (Blok A + Blok B) na isMe()/ME(), ostatni podetap Etapu 6d (dyplomacja, hot-seat).

ZARZUTY:

1. [KRYTYCZNY] Kluczowe twierdzenie o weryfikacji mutacyjnej Bloku A jest niereprodukowalne — bezpośrednio obalone. Docstring nowej bramki (`gra/tools/hotseat-etap6d-podetap-d-worldendturn-live-test.cjs:37-39`) stwierdza wprost: „podmiana pojedynczego `ME()` z powrotem na `0`/literal wewnątrz WYŁĄCZNIE Bloku A czerwieni WYŁĄCZNIE krok D". Wykonałem dokładnie tę mutację (main.ts:31245: `getDiploRelation(csOwnerId, ME())` → `getDiploRelation(csOwnerId, 0)`), zbudowałem (`vite build`) i uruchomiłem tę samą bramkę: wynik **12 pass · 0 fail** — identyczny z niezmutowaną bazą, ZERO zaczerwienienia, także w kroku D. Powtórzyłem analogicznie dla Bloku B (main.ts:31855: `getDiploRelation(ME(), ownerId)` → `getDiploRelation(0, ownerId)`): również **12 pass · 0 fail**. Przyczyna: `ME()` (main.ts:10467-10469) zwraca `humanSeats.activeHumanOwnerId`, zainicjalizowane na `HUMAN_OWNER_PRIMARY === 0` (`game/human-owners.ts:21`) i NIGDY nieprzełączane w tej bramce — jedyna funkcja zmieniająca ten stan, `switchActiveHuman` (main.ts:10530), jest wołalna WYŁĄCZNIE przez hak testowy `__hotSeatTestDebug` (main.ts:10527), którego ta bramka nigdzie nie używa. `ME()` jest więc w całym scenariuszu tej bramki dowiedlnie zawsze równe `0` — podmiana `ME()→0` jest mutantem RÓWNOWAŻNYM behawioralnie, którego żaden test grający na jednym fotelu nie jest w stanie wykryć z definicji. Rozróżnienie w raporcie Operatora między Blokiem A („zweryfikowano mutacyjnie") a Blokiem B („luka, nie domknięto") jest w tym świetle nieuzasadnione — żaden z bloków nie ma faktycznego dowodu, że `ME()` robi coś innego niż literał `0` dzisiaj. Dodatkowo sprawdziłem odmienny wariant mutacji (podmiana argumentu na „zły" nieistniejący owner `99`, a nie na `0` — zgodnie z sformułowaniem z treści commita „ME()->literal zly owner", innym niż dosłowny tekst docstringu bramki): mutacja `setDiploRelation(csOwnerId, ME(), newRel)` → `setDiploRelation(csOwnerId, 99, newRel)` w gałęzi egzekucji DOW Bloku A RZECZYWIŚCIE czerwieni bramkę (10 pass · 2 fail, krok D i konsola). To pokazuje, że bramka ma pewną czułość na grubą korupcję argumentu ownera, ale nie rozwiązuje problemu: nie jest to test tego, czy migracja `0→ME()` została wykonana poprawnie (bo `0` i `ME()` są dziś identyczne), tylko test tego, czy WYSTARCZAJĄCO inny literał psuje wynik — co nie odpowiada na pytanie zlecone w tej rundzie. Wniosek: twierdzenie „mutacja niezależnie czerwieni tylko krok dotyczący Bloku A" (raport Operatora, akapit 3) jest jako opisane FAŁSZYWE/niereprodukowalne i wymaga korekty albo dostarczenia rzeczywistego dowodu z przełączeniem fotela (`__hotSeatTestDebug.switchActiveHuman`), które faktycznie odróżnia `ME()` od `0`.

2. [DROBNY] Niespójność liczby literałów w Bloku B: raport Operatora i treść commita (`8051be5a`) podają „20 literałów 0 → ME()" dla Bloku B, ale własna wyliczanka funkcji w tym samym akapicie (getDiploRelation×1, setDiploRelation×2, sumArmyMForOwner×1, objectivePowerByOwner×1, dealInvolvesOwners×1, resolvePokojTrustTier×1, civKeyForOwner×2, ownerReligionForOwnerId×1, cities.filter×2, ownersShareLandBorderLive×1, getWiarygodnosc×1, citiesHaveTradeConnection×1, hasActiveResourceTradeDealForPair×2, pickResourceTradeRelOffer×1, partnerId string×1, hasTreaty×1, isPeaceLockedBetween×1) sumuje się do **21**, co potwierdza niezależny `grep -c 'ME()'` na zakresie linii 31850-31948 w scalonym pliku (wynik: 21). Sama migracja jest kompletna (potwierdzone zero pozostałych literałów `0` reprezentujących gracza w obu blokach — zarzut nie dotyczy jakości kodu, tylko dokładności liczby w raporcie/commicie).

TESTY (wykonane niezależnie w /home/user/wt-6d-PODETAP-D):
- `npx tsc --noEmit` (tsc 5.9.3, zgodnie z wymogiem repo): 0 błędów — potwierdzone dwukrotnie (przed i po testach mutacyjnych, plik przywrócony bajt w bajt między próbami, md5sum zgodny).
- Odczytane w całości oba bloki po zmianie: Blok A dokładnie main.ts:31238-31293 (13× `ME()`), Blok B dokładnie main.ts:31850-31948 (21× `ME()`, nie 20 — zarzut #2) — granice zgodne z raportem Operatora co do linii.
- Zero pozostałych literałów `0` reprezentujących gracza w obu blokach (pozostałe `0` to: `startOi === 0` — indeks wznowienia pętli, niezwiązany z ownerem gracza; `?? 0` fallbacki długości/mocy; `: 0`/`: 1` ternary; próg porównania `> 0` — żaden nie koduje ID gracza).
- Styk z P-WOJNA-EPOKI-NAJTRUDNIEJSZY-NIE-WYBUCHA-Q1: `isForcedEpochWarDeclareCmd`/`dipLayerIgnoringPlayerFog` leżą w main.ts:32270-32440, poza obiema granicami (Blok B kończy się 31948, odstęp 324 linie) — zero styku potwierdzone. `forced-war-player-no-contact-live-test`: **14/14 pass**, naprawa nienaruszona.
- Żywa bramka Chromium (`hotseat-etap6d-podetap-d-worldendturn-live-test.cjs`): uruchomiona wielokrotnie na realnym `doStartGame`/`endTurn()`/`Math.random` — baza **12 pass · 0 fail** (dwa niezależne przebiegi, discovered=[15,16,17] powtarzalnie, tura wyzwolenia wojny 21 i 24 — spójne z realnym RNG, nie symulacją). Potwierdzam: to jest realna gra (Chromium, vite build, brak podmiany RNG), nie reimplementacja.
- Mutacje wykonane niezależnie i zrewertowane (plik przywrócony, `git status` czysty, md5sum identyczny z bazą): patrz Zarzut #1 — literalny revert `ME()→0` w Bloku A i w Bloku B: oba **NIE czerwienią** bramki (12/0 oba razy). Kontrolna mutacja na „zły owner" (99) w gałęzi egzekucji DOW Bloku A: **czerwieni** (10 pass, 2 fail).
- Wszystkie znalezione bramki forced-war/dyplomacji (23 pliki, szerzej niż wymienione 18 z dyspozycji — uruchomione wszystkie pasujące): `ai-war-gate-test` 24/24, `diplomacy-layers-test` 22/22, `diplomacy-war-gates-test` 19/19, `forced-war-bronze-main-guard-test` 28/28, `forced-war-bronze-new-game-reset-test` 34/34, `forced-war-bronze-test` 56/56, `forced-war-iron-main-guard-test` 37/37, `forced-war-iron-mutant-probe` zielone (55/55 + 37/37 asercji zaczerwienionych pod mutacją, źródła przywrócone), `forced-war-iron-test` 55/55, `forced-war-reguly-multi-turn-simulation-test` 39/39, `forced-war-stone-main-guard-test` 19/19, `forced-war-stone-test` 38/38, `forced-war-trojstronna-main-guard-test` 14/14, `forced-war-trojstronna-test` 23/23, `p-wojna-wymuszona-trzy-naprawy-test` 13/13, `wojna-wymuszona-parowanie-test` 47/47, `wojna-wymuszona-prog-tury-gracz-test` 9/9, `forced-war-player-pre-contact-gate-test` 45/45, `forced-war-iron-era-enter-turn-save-load-test` 20/20, `forced-war-player-no-contact-live-test` 14/14, `forced-war-player-target-live-test` 12/12, `forced-war-iron-player-target-live-test` 12/12, `forced-war-trojstronna-domino-live-test` 31/31. Zero regresji.
- 5 bramek referencyjnych: `logic-test` 213/213, `tech-tree-test` 19/19, `research-test` 33/33, `unit-replace-test` 13/13, `combat-test` 6/6 — identyczne z §6.
- `git diff --stat 770d0078 HEAD`: wyłącznie `gra/src/main.ts` (66 linii, dokładnie wewnątrz obu zadeklarowanych granic — potwierdzone czytaniem pełnego diffa) + nowy plik `gra/tools/hotseat-etap6d-podetap-d-worldendturn-live-test.cjs` (189 linii, nowy plik testowy) — zgodne z allowlistą, brak dotknięcia reszty `runWorldEndTurn` ani innych funkcji. `git status` w worktree czysty przed i po mojej weryfikacji.

BLOKADY: Zarzut #1 (krytyczny) wymaga odpowiedzi Operatora z dowodem z wytworu (per R-PROC-AUTOBOT §3c) — albo korekta twierdzenia o weryfikacji mutacyjnej (uznanie, że dla obu bloków to dziś inspekcja kodu + brak regresji, nie dowód behawioralny, dopóki `ME()` jest no-opem), albo dostarczenie rzeczywistego testu z przełączeniem fotela (`__hotSeatTestDebug.switchActiveHuman`) faktycznie odróżniającego `ME()` od `0`.

RUNDY: 1/5
NASTĘPNY KROK: Zarzuty wracają do Operatora (ten sam wykonawca, drugie wywołanie — obrona per zarzut z dowodem z wytworu, R-PROC-AUTOBOT §3c) przed ponownym Evaluatorem/Final Control.
DEPLOY/PUSH: NIE WYKONANO
