# R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1 — Evaluator, runda 1/5

**Uwaga proceduralna:** agent Evaluator zwrócił kompletną treść tego raportu przez
StructuredOutput, ale proces zakończył się przed zapisaniem/zacommitowaniem pliku na dysku.
Orkiestrator odtwarza ten raport z zarejestrowanej treści zwrotnej agenta, bez zmiany
choćby jednego znaku, i commituje w jego imieniu.

STATUS: DECISION_REQUIRED
DOMAIN: GAME
TEMAT: R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1
GOAL: wygładzić `prawo_bonus_osiedle_pop` analogicznie do G10 Szczęścia, redukując najgorszy spadek PorPct na +1 mieszkańca
ZMIANY-COMMIT: worktree `/home/user/wt-szczescie-c`, gałąź `autobot/R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1`, HEAD `55a346a7`. Niezależnie zweryfikowane `git diff origin/main..HEAD --stat`: wyłącznie `gra/data/society-params.json` (26 linii, klucz `prawo_bonus_osiedle_pop` + jego `opis`), nowa bramka `gra/tools/szczescie-audyt-c-prawo-osiedla-test.cjs`, i pliki `dyspozycje/autobot/runs/.../*`. Zero naruszeń allowlisty potwierdzone.
TESTY (własny harness, esbuild + realny `evaluateOrderFromBreakdown`, na tym samym module co Operator, ale SZERSZA siatka — pełny zbiór potęgowy 6 flag administracji (64 kombinacji, nie tylko pojedyncze flagi jak u Operatora) × `palacTier` 0-3 (Operator w ogóle nie wariował tego pola) × garnizon × wojna × stolica-easy × udział kultury/religii/luksusu (Operator trzymał te pola na stałych wartościach neutralnych)):
- `tsc --noEmit`: 0 błędów — potwierdzone niezależnie.
- 5 bramek referencyjnych: logic 213/213, tech-tree 19/19, research 33/33, unit-replace 13/13, combat 6/6 — potwierdzone niezależnie, identyczne liczby.
- Nowa bramka `szczescie-audyt-c-prawo-osiedla-test.cjs`: 15/15 OK — potwierdzone.
- Proporcje między trudnościami (easy≥normal≥hard na każdym z 4 indeksów) — potwierdzone programowo, zachowane.
- Stabilność pop≥5: potwierdzone niezależnie identyczne PRZED/PO = 2,2 p.p. (scenariusz kanoniczny Operatora) — zero regresji.
- Cała rodzina Prawo/Porządek/Szczęście/Order/Society (16 bramek) uruchomiona niezależnie: identyczne liczby jak w raporcie Operatora na każdej — w tym oba nowe czerwone (`szczescie-skala-normalizacja-test` 4 FAIL, `society-breakdown-test` 6 FAIL) i oba pre-istniejące czerwone (`border-march-wygasanie-test` 4 FAIL, `szczescie-przebudowa-skali-test` 4 FAIL). Przyczynowość potwierdzona NIEZALEŻNIE metodą kopii pliku (nie `git checkout`, zgodnie z SS2b): podmieniony na prawdziwą starą treść `gra/data/society-params.json` z `git show 8031bf5f`, dwie bramki NOWE dają 0 FAIL na tej prawdziwej bazie (148/148 i 53/53), dwie pre-istniejące dają identyczne 4 FAIL także na bazie — plik przywrócony kopią, `git diff --quiet` czyste.
- Najgorszy spadek PorPct — własna, SZERSZA siatka niż Operatora znajduje wynik GORSZY niż zgłoszony: PRZED 28,0 p.p. (Operator: 27,1), PO 20,0 p.p. (Operator: 19,3), podłoga przy całkowitym wyzerowaniu klucza 16,5 p.p. (Operator: 15,8) — patrz ZARZUT 1.
BLOKADY: DECISION_REQUIRED (potwierdzony, zasadny) — dwa punkty z `decision-abc.md`, oba zweryfikowane jako prawdziwe i poprawnie zakwalifikowane jako wymagające decyzji właściciela, nie do rozstrzygnięcia przez Operatora/Evaluatora/Final Control.
RUNDY: 1/5
NASTĘPNY KROK: Zarzuty niżej do Obrony Operatora (ta sama runda, §3c) — po Obronie Final Control. Decyzja właściciela w `decision-abc.md` (magnitude + zakres allowlisty) pozostaje odrębna i nadrzędna wobec wyniku Final Control tej rundy.
DEPLOY-PUSH: NIE WYKONANO

ZARZUTY:

1. **Niepełna siatka pomiarowa zaniża zgłoszoną wartość najgorszego przypadku o ~0,7 p.p. w obu kierunkach (PRZED i PO)** — `gra/tools/szczescie-audyt-c-prawo-osiedla-test.cjs`, funkcja `measureWorstDrop` (linie 104-146), zmienne `ADMIN_VARIANTS` (linie 75-80, tylko 4 kombinacje: po jednej fladze administracji naraz lub żadna — nigdy kilka naraz) i brak jakiejkolwiek wariacji `palacTier`/`hasPalac`, `ownCultureShare`, `ownReligionShare`, `podzialHandlu`/luksusu, `stolicaEasyBonus` (te pola nie występują w `lawInput`/`happinessInput` budowanym w tej bramce w ogóle). Niezależny harness z pełnym zbiorem potęgowym 6 flag administracji (64 kombinacji) × `palacTier` (0-3) × wariacją udziału kultury/religii/luksusu/stolicy-easy znajduje: PRZED = **28,0 p.p.** (easy/era1/pop4→5, np. `hasDomStarszyzny`, brak Pałacu, `ownCultureShare=0,5, ownReligionShare=1, luksPct=100`) wobec zgłoszonych 27,1 p.p.; PO = **20,0 p.p.** (ten sam punkt, kilka równoważnych kombinacji administracji z Pałacem tier 2 lub bez Pałacu + Sąd/Garnizon-budynek) wobec zgłoszonych 19,3 p.p.; podłoga przy `factor=0` = **16,5 p.p.** wobec zgłoszonych 15,8 p.p. Ma to znaczenie, bo binarne kryterium sukcesu z `00-dispatch.md` wymaga "dokładnej, zmierzonej wartości najgorszego przypadku" w **całej** siatce parametrów, nie w podzbiorze wybranym przez Operatora — a własna bramka Operatora (asercja nr 3, próg `< 20`) w tym świetle jest ustawiona tuż PONIŻEJ prawdziwego maksimum globalnego (20,0), nie nad nim; test przechodzi tylko dlatego, że mierzy węższą siatkę niż istnieje naprawdę. Nie zmienia to wniosku (nadal rząd wielkości > precedensu 6 p.p., DECISION_REQUIRED nadal zasadny) — a zatem klauzula reguły przeciw samooszukiwaniu „operator nie zgłosił jako DECISION_REQUIRED" nie ma tu zastosowania wprost — ale zgłoszona liczba nie jest prawdziwym maksimum, więc formalnie nie spełnia kryterium sukcesu dosłownie ("dokładna... wartość najgorszego przypadku").

2. **Niespójność współczynnika skalowania w jednej komórce tablicy `hard`** — `gra/data/society-params.json`, blok `prawo_bonus_osiedle_pop.hard`, indeks 3 (pop=4): wartość `1` odpowiada faktorowi `1/6 ≈ 0,167` względem starej wartości `6`, podczas gdy pozostałe 11 komórek (wszystkie pozostałe indeksy easy/normal/hard) mieszczą się w paśmie faktora `0,25–0,32`, spójnym z deklarowanym w `opis` i w raporcie "factor ~0,3". Przy standardowym zaokrągleniu do najbliższej liczby całkowitej `6×0,3=1,8` powinno dać `2`, nie `1`. Nie jest to naruszenie żadnego twardego kryterium (tablica `hard` pozostaje malejąca i dodatnia pop1→4, `easy[3]=3 ≥ normal[3]=2 ≥ hard[3]=1` nadal zachowane, a mniejsza wartość jest bardziej, nie mniej, konserwatywna względem celu redukcji urwiska) — ale twierdzenie "przeliczone proporcjonalnie" w `opis` i w `01-operator.md` jest nieścisłe dla tej jednej komórki.
