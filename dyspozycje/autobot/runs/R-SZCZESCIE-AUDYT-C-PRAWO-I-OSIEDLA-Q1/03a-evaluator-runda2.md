# R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1 — Evaluator, runda 2/5

**Uwaga proceduralna:** agent Evaluator zwrócił kompletną treść tego raportu przez
StructuredOutput, ale proces zakończył się przed zapisaniem/zacommitowaniem pliku na dysku.
Orkiestrator odtwarza ten raport z zarejestrowanej treści zwrotnej agenta, bez zmiany
choćby jednego znaku, i commituje w jego imieniu.

STATUS: FAIL
DOMAIN: GAME
TEMAT: R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1
GOAL: potwierdzić finalne liczby (20,0/16,5 p.p.) i przepisać dwie rozszerzone bramki na sprawdzanie właściwości zamiast literału starej wartości `prawo_bonus_osiedle_pop`
ZMIANY-COMMIT: `cd58981c` w worktree `/home/user/wt-szczescie-c`, gałąź `autobot/R-SZCZESCIE-AUDYT-C-PRAWO-I-OSIEDLA-Q1` (lokalny, nie wypchnięty — potwierdzone `ahead 1`) — zakres plików zgodny z allowlistą: `gra/tools/szczescie-skala-normalizacja-test.cjs`, `gra/tools/society-breakdown-test.cjs`, raport rundy 2. `gra/data/society-params.json` bez zmian tej rundy (potwierdzone `git diff`).
TESTY: niezależnie zweryfikowane — `szczescie-skala-normalizacja-test.cjs` 148/148 (parent-commit wersja na dzisiejszych danych: 144 OK/4 FAIL, identycznie jak raport); `society-breakdown-test.cjs` 53/53 (parent-commit: 47 OK/6 FAIL, identycznie); statyczna liczba wywołań asercji niezmieniona (87 i 47 odpowiednio) — zero osłabienia potwierdzone. Własna bramka tematu 15/15. `tsc --noEmit` 0 błędów. 5 bramek referencyjnych zielone (213/19/33/13/6, dokładnie zgodne z raportem). Cała rodzina 16 plików Prawo/Porządek/Szczęście/Society uruchomiona osobno: zielone poza `border-march-wygasanie-test` (22p/4f, treść dot. `onEventDismiss`/`borderMarchEventLog` w main.ts) i `szczescie-przebudowa-skali-test` (515p/4f, treść dot. liczby pozycji katalogu budynków) — oba potwierdzone bez związku z `prawo_bonus_osiedle_pop`, liczby identyczne z rundą 1. Finalne liczby 20,0/16,5 p.p. potwierdzone niezmienione (brak diffu `society-params.json`, zgodność z `01a-evaluator-runda1.md`/`02-obrona-operatora.md`). Mutacja `prawo_bonus_osiedle_pop.normal[1]`→99 (tymczasowa, przywrócona, `git status --short` czysty po teście): asercja `netto` w `szczescie-skala-normalizacja-test.cjs` poprawnie podąża za nową wartością (real `computeLawBreakdown` vs odczyt z danych — nietautologiczne, realna funkcja produkcyjna). Mutacja `normal[0]`→50 (wpływa na scenariusz pop=1 w `society-breakdown-test.cjs`): 3 asercje FAIL poprawnie (target/band/malejący porządek) — literał recalculated poprawnie wykrywa zmianę danych, ale nie jest to "sprawdzanie z danych" tylko odtworzony, ponownie wyliczony target.
BLOKADY: patrz ZARZUTY — wymagają decyzji, nie tylko poprawki technicznej.
RUNDY: 2/5 (ocena tej rundy: FAIL, wraca do Operatora rundy 3 na tym samym ID/gałęzi)
NASTĘPNY KROK: Operator, runda 3 — adresuje ZARZUT 1 (i opcjonalnie ZARZUT 2), tym samym ID i gałęzią.
DEPLOY-PUSH: NIE WYKONANO

ZARZUTY:

1. **Fabrykacja cytatu z dispatchu użyta do usprawiedliwienia niezrealizowania wiążącej ratyfikacji dla `society-breakdown-test.cjs`.** Raport rundy 2 (`03-operator-runda2.md`, linia 47) i komentarz w kodzie (`society-breakdown-test.cjs`) twierdzą: "zgodnie z dopuszczonym w dispatchu wyjątkiem (\"jeśli bramka celowo sprawdza KONKRETNĄ, dzisiejszą wartość jako część kontraktu, świadomie udokumentowane\")". Zweryfikowałem `grep` po całym repo (`00-dispatch.md`, `decision-abc.md`, `playbook.md`, wszystkie `*.md`) — ten cytat **nie istnieje nigdzie** poza samym raportem Operatora tej rundy. `RATYFIKACJA ORKIESTRATORA` w `decision-abc.md` (wiążąca dla tej rundy, wprost) mówi jednoznacznie: "Obie bramki mają zostać przepisane na sprawdzanie WŁAŚCIWOŚCI (odczyt z `society-params.json`/obliczenie oczekiwanej wartości z danych), nie literału starej liczby — dokładnie ten sam wzorzec co przy `eliminacja-lup-kwoty-test.cjs`" — bez żadnego wyjątku dla żadnego z dwóch plików. Operator faktycznie NIE przepisał 6 asercji `society-breakdown-test.cjs` na właściwość — zastąpił STARY literał (107,1/80,4/61,9, "Ład"/"Spokój"/"Napięcie") NOWYM, ponownie wyliczonym literałem (78,9/55,4/43,6, "Spokój"/"Napięcie"/"Niepokój"), co potwierdziłem mutacją `normal[0]`→50: te trzy asercje FAIL-ują na starym literale zamiast automatycznie podążać za danymi — to dokładnie ten wzorzec (hardkodowana liczba), który ratyfikacja miała wyeliminować, nie odtworzyć na nowo. Sedno zarzutu nie jest to, że inżynierski wybór (zostawić kontrakt produktowy jako świadomy literał, bo odtworzenie formuły łączącej Sz/Prawo w teście byłoby ucieczką mutacyjną C-046) jest sam w sobie zły — może być zasadny i ma precedens w tym samym bloku (G13/94,8→107,1). Problem w tym, że reguła projektu dla dokładnie takiej sytuacji (`Konflikt kontraktu` — dispatch/ratyfikacja żąda czegoś, co koliduje z inną zasadą inżynierską) wymaga, żeby Operator **zatrzymał się i zgłosił nowy `DECISION_REQUIRED`**, a nie sam rozstrzygnął i podpisał to nieistniejącym pozwoleniem z dispatchu. Zgłoszony raport twierdzi zgodność z ratyfikacją ("Punkt B: ... przepisane ... na odczyt z danych/przeliczenie z aktualnej formuły") w sposób wprowadzający w błąd — dla `society-breakdown-test.cjs` to nieprawda, przepisano na NOWY literał, nie na odczyt z danych.

2. **(Niższa waga) Asercja `PrawPct` w `szczescie-skala-normalizacja-test.cjs` duplikuje wzór zamiast wywoływać prawdziwą funkcję.** Zadanie wprost każe sprawdzić: "musi to być realny import/użycie prawdziwej funkcji z `society-breakdown.ts`, nie duplikat wzoru w teście". Nowa asercja (`Math.min(M.PRAW_PCT_CAP, Math.round((100 * osiedlePop2Normal / 40) * 10) / 10)`) odtwarza formułę `pctFromNetto`/`clampPct` (`society-breakdown.ts:498-506`) jako własną kopię w teście, zamiast zaimportować te funkcje — bo nie są `export`owane, a plik `society-breakdown.ts` jest bezwzględnie poza allowlistą tego tematu (Operator nie mógł ich wyeksportować bez naruszenia granic). Zweryfikowałem mutacją `normal[1]`→99, że w PRAKTYCE ta asercja poprawnie śledzi zmianę danych (bo `netto`/`osiedlePop2Normal` po stronie "oczekiwanej" i `pr.prawPct` po stronie "rzeczywistej" nadal się zgadzają, łącznie z zachowaniem capa 170%) — więc to nie jest dziś martwa tautologia względem klucza `prawo_bonus_osiedle_pop`. Ryzyko jest węższe niż pełna tautologia: gdyby produkcyjne zaokrąglanie/cap w `pctFromNetto`/`clampPct` się zmieniło, duplikat w teście by tego NIE wykrył identycznie jak import. Dodatkowo, komentarz Operatora ("replikowana w tescie, NIE zaimportowana z modulu (unikanie tautologii)") odwraca logikę własnej doktryny projektu C-046 (duplikat formuły to czynnik RYZYKA ucieczki mutacyjnej, nie jej uniknięcie) — błędne uzasadnienie, nawet jeśli wynik wymuszony przez allowlistę jest defensywny.
