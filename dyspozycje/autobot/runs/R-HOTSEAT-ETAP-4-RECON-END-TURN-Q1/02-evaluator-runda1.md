STATUS: PASS-WITH-NOTES
DOMAIN: INFORMATIONAL
TEMAT: R-HOTSEAT-ETAP-4-RECON-END-TURN-Q1
GOAL: Ocena kompletności/trafności analizy Operatora (recon `triggerPlayerEndTurn()` + plan Etapu 4) — wykonana, samodzielny odczyt main.ts (worktree `/home/user/wt-hotseat-etap4-recon`, ten sam plik/stan co Operator) potwierdza zdecydowaną większość mapy, ale znajduje 5 konkretnych luk nieujętych w dokumencie Operatora.
ZMIANY/COMMIT: brak — `git status --short` w worktree pokazuje wyłącznie katalog `dyspozycje/autobot/runs/R-HOTSEAT-ETAP-4-RECON-END-TURN-Q1/` jako untracked, zero zmian w `gra/src/**`.
TESTY: nie dotyczy (recon/ocena dokumentu).
BLOKADY: brak formalnych — 5 zarzutów niżej do decyzji orkiestratora/uzupełnienia w rundzie 2.
RUNDY: 1/5
NASTĘPNY KROK: Operator uzupełnia dokument (round 2) o pięć znalezisk niżej PRZED napisaniem planu implementacji, albo orkiestrator jawnie akceptuje je jako dług przeniesiony do rundy 2 — potem Final Control.

ZARZUTY:

1. **Faza 1 (28519-28579) opisana nieściśle — pominięta pętla bez filtra ownera, analogiczna do Ryzyka #1.** Operator klasyfikuje całą fazę 1 jako „WSZYSTKO odnosi się do `anim`" (sekcja 1, wiersz 1 tabeli). To nieprawda: main.ts:28525-28528 —
   ```ts
   for (const st of cityOrderState.values()) {
     if (st.bunt) st.bunt = undefined;
   }
   ```
   — czyści flagę buntu dla **WSZYSTKICH miast w grze** (`cityOrderState` to `Map<string,OrderState>` keyowana po `city.id`, main.ts:4303 — zero filtra ownera), nie ma nic wspólnego z `anim`. To dokładnie ta sama struktura co Ryzyko #1 (kod „światowy" osadzony tekstowo w bloku „gracza", przed `turn++`) — świeżo zweryfikowałem, że jest to jedyne miejsce czyszczące `st.bunt` w całym pliku (drugie i jedyne inne wystąpienie, main.ts:14375, tylko CZYTA flagę). Efekt praktyczny prawdopodobnie nieszkodliwy przy podwójnym wywołaniu w hot-seat (ten sam argument idempotencji, co Operator dał dla `evictForeignUnitsFromCityHexes` — między dwoma wywołaniami w tej samej turze świata ekonomia jeszcze nie ustawiła nowych flag), ale Operator nigdy tego fragmentu nie nazwał, nie sklasyfikował ani nie wymienił jako kandydata do przesunięcia/przemyślenia w rundzie 2 — mimo że sam go zacytował pośrednio w zakresie linii. Dług z sekcji 7 pkt 4 wymienia wyłącznie `runPlannedMarchesAtPlayerEndTurn()` jako niesprawdzoną funkcję z zewnątrz; ten fragment nie wymagał wcale wywołania funkcji z zewnątrz — był już w przeczytanym tekście.

2. **Pominięty drugi przypadek klasy Ryzyka #4: `pendingAutoRationForNextTurn`.** main.ts:28908 (wewnątrz bloku ekonomii, faza 10): `if (ownerId === 0) { pendingAutoRationForNextTurn = autoRationResult; }` — ustawiane WYŁĄCZNIE dla ownera 0. Odczytywane/flush @ main.ts:32915-32920 (`if (pendingAutoRationForNextTurn?.adjusted) { rationAutoEventLog.unshift(...); ...; pendingAutoRationForNextTurn = null; }`), TUŻ PRZED `finally`. To dokładnie ten sam wzorzec co Ryzyko #4 (`deferredPlayerUnitRevealIds`, hardkodowane na ownerId 0) — w hot-seat auto-wyrównanie racji żywnościowych dla człowieka #2 nigdy nie ustawi tej zmiennej, więc komunikat HUD „Auto-dostosowano racje" nigdy się nie pojawi dla człowieka #2, mimo że mechanizm faktycznie dla niego zadziałał (bo `onlyAutoManaged`/`requireFlowBalance` w `autoBalanceRationsToSolvency` same są `ownerId === 0`-owe tylko częściowo, ale `adjusted` liczy się dla KAŻDEGO ownera — flaga notyfikacji jest tym, co jest zawężone). Operator nie znalazł tego, bo grep w sekcji 3 (`preBattle|deferredEotHints|deferredMergePrompts|flushDeferredPlayerUnitReveals|deferredPlayerUnitRevealIds|aiTurnAwaitingBattle|aiCmdResume =`) nie zawierał słowa „pending".

3. **Ten sam wzorzec istnieje też w `deferredMergePrompts` — identyfikatorze, który Operator JUŻ wymienia w tabeli czterech (sekcja 3), ale bez zauważenia bramki ownera.** `promptMergeIfCoLocated` (funkcja zasilająca `deferredMergePrompts.push` @ main.ts:10928, cytowane przez Operatora) ma na wejściu main.ts:10909: `if (!rep || rep.ownerId !== 0) return;` — twarde zawężenie do ownera 0, PRZED warunkiem `endTurnInProgress` które Operator cytuje. Operator cytuje fragment kodu zaczynający się od komentarza „P-KONIEC-TURY-ZDARZENIA-NACHODZA-NA-SIEBIE" (kilka linii niżej), pomijając ten wcześniejszy guard we własnym cytacie — więc w hot-seat prompt scalenia armii dla jednostek człowieka #2 nigdy nie trafi do kolejki `deferredMergePrompts` (funkcja ucina się na `rep.ownerId !== 0` zanim dojdzie do logiki odkładania). Ten sam typ ryzyka co #4, dla identyfikatora już nazwanego w dokumencie.

4. **Zależność między fazą „gracza" a fazą „świata" pominięta w planie cięcia (GOAL pkt d): zmienna `nextTurnNum`.** `const nextTurnNum = turn + 1;` obliczana raz, main.ts:28521 (w zasięgu, który operator klasyfikuje jako fazę 1/„gracz"), używana WIELOKROTNIE w fazach 8-13, czyli dokładnie tych, które sekcja 6.1 Krok 0 planuje dosłownie wyciąć do nowej funkcji `runWorldEndTurn()` zaczynającej się „OD `turn++`": `setTurnTransition(10,...,nextTurnNum)` @28692, `(14,...)` @28703, `(38,...)` @30426, `(94,...)` @32480, `(98,...)` @32806. Plan Kroku 0 („dosłowny copy-paste bez zmiany semantyki... fazy 7-14") nie wspomina, że `nextTurnNum` trzeba albo przekazać jako parametr do `runWorldEndTurn()`, albo przeliczyć na nowo na jej starcie (`const nextTurnNum = turn + 1` PRZED inkrementacją wewnątrz tej nowej funkcji) — bez tego „dosłowny" copy-paste zostawia martwe odwołanie do zmiennej z zewnętrznego domknięcia. Drobne z punktu widzenia trudności naprawy (jedna linijka), ale to jest dokładnie ta klasa zależności między-fazowej, o którą pyta GOAL (d), i Operator jej nie wymienia mimo że deklaruje krok „dosłowny copy-paste" jako bezpieczny bez zastrzeżeń.

5. **Drobna nieścisłość liczbowa w sekcji 3.** Operator: „`flushDeferredAutoPreBattle()` wołana WIELOKROTNIE w całym pliku (11 miejsc)". Świeży grep (`grep -n "flushDeferredAutoPreBattle()" gra/src/main.ts`) daje 12 trafień, z czego 2 są liniami komentarza opisowego (main.ts:28325, 28341 — tekst „...flushDeferredAutoPreBattle() hasn't fired..."), więc faktycznych wywołań jest **10**, nie 11. Nie wpływa na żaden wniosek dokumentu, ale liczba była przedstawiona jako wynik świeżego grepu, a nie zgadza się z ponownym przeliczeniem.

Pozycje NIE zakwestionowane (zweryfikowane niezależnie i potwierdzone zgodne z dokumentem Operatora): granice funkcji (28497-32966, 4469 linii), lokalizacja `turn++` (28647, jedyne wystąpienie), wszystkie 16 granic fazowych w tabeli sekcji 1 (zweryfikowane grepem `setTurnTransition` dla każdego progu procentowego), Ryzyka #1-#4 jako takie (treść i lokalizacje zgadzają się co do linii i mechanizmu), dokładnie 3 miejsca wywołania `triggerPlayerEndTurn()` poza definicją (20910, 21498, 33092 — potwierdzone pełnym grepem całego pliku), struktura rekurencyjna `runAiPhase`/race z `endTurnInProgress` (32105, 32766, komentarz 32938-32952), oraz klasyfikacja `runPlannedMarchesAtPlayerEndTurn()` jako czysto [GRACZ] (samodzielnie przeczytałem ciało @23143/23235 — `executePlannedMarchesEndTurn`/`applyMarchSegmentInstant` filtrują `ownerId===0` explicite, więc „dług" z sekcji 7 pkt 4 jest w praktyce rozstrzygnięty na korzyść klasyfikacji Operatora, tylko sam Operator tego nie sprawdził).

DEPLOY/PUSH: NIE WYKONANO
