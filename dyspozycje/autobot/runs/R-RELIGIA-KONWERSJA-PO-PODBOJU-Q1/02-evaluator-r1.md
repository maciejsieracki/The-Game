# R-RELIGIA-KONWERSJA-PO-PODBOJU-Q1 — Evaluator, runda 1/5

**Uwaga proceduralna:** agent Evaluator zwrócił kompletną treść tego raportu przez
StructuredOutput, ale proces zakończył się przed zapisaniem/zacommitowaniem pliku na dysku.
Orkiestrator odtwarza ten raport z zarejestrowanej treści zwrotnej agenta, bez zmiany
choćby jednego znaku, i commituje w jego imieniu.

STATUS: FAIL
DOMAIN: GAME
TEMAT: R-RELIGIA-KONWERSJA-PO-PODBOJU-Q1
GOAL: `onCityCapturedReligion` jako wierny odpowiednik `onCityCapturedCulture` (inwersja `1-prevShare` przełożona na `counts`), wywołanie bezwarunkowe z `main.ts`, dowód że `convertViaTemple` rusza po naprawie.
ZMIANY-COMMIT: `1217bf16` (worktree `/home/user/wt-religia`, gałąź `autobot/R-RELIGIA-KONWERSJA-PO-PODBOJU-Q1`, baza `ba2fde99`) — zweryfikowany niezależnie: `git diff --stat` ograniczony do 5 plików allowlisty, `culture-religion.ts` diff to czysty dodatek (zero usuniętych linii poza nagłówkiem diffa → `religionHappiness`/ECHO pkt.3 nietknięte, potwierdzone bez reimplementacji), `main.ts` diff dokładnie w bloku 26938–26968, `isBarbarian` guard bez zmian.
TESTY: Niezależnie uruchomione i zgodne z raportem Operatora: `tsc --noEmit` czysto; `religia-konwersja-po-podboju-test.cjs` 9/9; `culture-religion-test.cjs` 65/0; `logic-test` 213/213, `tech-tree-test` 19/19, `research-test` 33/33, `unit-replace-test` 13/13, `combat-test` 6/6; cała rodzina religia/kultura/capture/podboj/conquest (`ai-city-capture-integration-test` 14, `empire-religia-panel-coverage-test` 15/15, `post-capture-law-test` 25/0, `capital-capture-test` 86/86) zielone; `conquest-stability-test` 28/1 i `barb-city-capture-cluster-test` 92/1 — obie FAIL potwierdzone przeze mnie jako identyczne na czystym `main`/`ba2fde99` (uruchomione bezpośrednio w `/home/user/The-Game`, bez `git stash`), zero związku z tematem. Punkt 3 ECHO (binarność) zweryfikowany grepem, kod nietknięty. Dowód wieloturowy `convertViaTemple` (0.3→0.48 monotonicznie) odtworzony niezależnie przez odczyt bramki, logika poprawna.

Własna kontrola (REGULA PRZECIW SAMOOSZUKIWANIU, miasto z 3+ religiami w `counts` przed podbojem) wykryła DWA potwierdzone defekty w samej formule redystrybucji `remaining` w `onCityCapturedReligion` (`gra/src/game/culture-religion.ts` ~789–821) — patrz ZARZUTY.
BLOKADY: brak (defekty są naprawialne bez zmiany zakresu/allowlisty).
RUNDY: 1/5
NASTĘPNY KROK: Operator, RUNDA 2 (poprawka formuły redystrybucji w `onCityCapturedReligion`, na tym samym ID/gałęzi).
DEPLOY-PUSH: NIE WYKONANO

ZARZUTY:

1. [CONFIRMED] Redystrybucja `remaining` między "trzecimi" religiami (`gra/src/game/culture-religion.ts` linie ~805–813) łamie zachowanie populacji, gdy w mieście jest **3 lub więcej** religii "innych" niż stara/nowy właściciel — schemat "`Math.round` dla wszystkich kluczy poza ostatnim, ostatni dostaje `remaining - assigned`" nie zabezpiecza przed przeciążeniem: gdy skumulowane zaokrąglenia kluczy nie-ostatnich PRZEKRACZAJĄ `remaining`, ostatni klucz wychodzi ujemny, guard `if (amount > 0)` go po cichu pomija — ale NIE koryguje już przypisanych (za dużych) wcześniejszych kluczy, więc suma końcowa jest WIĘKSZA niż populacja (ludzie "z niczego"). Reprodukcja (własny scenariusz, 5 religii w `counts` przed podbojem): `before = {counts:{keltyzm:2, a:1, b:1, c:1, d:1}}`, `population=6`, podbój różny okrąg, `previousOwnerReligion='keltyzm'`, `newOwnerReligion='rzym_bogowie'` → `after = {a:1, b:1, c:1, rzym_bogowie:4}`, suma = **7**, populacja = **6** (klucz `d` zjadła ujemna reszta i zniknęła, ale `assigned` z poprzednich kluczy i tak zostały wpisane). To bezpośrednie naruszenie wymogu „suma = populacja" z REGULA PRZECIW SAMOOSZUKIWANIU, i jest to całkowicie realny przypadek gry (miasto z wieloma wyznaniami po `spreadReligion` sąsiadów — dokładnie scenariusz opisany w komentarzu samej funkcji, linie 756–758), nie egzotyczny róg. Bramka Operatora (`religia-konwersja-po-podboju-test.cjs`) testuje wyłącznie JEDNĄ trzecią religię (druidyzm) — nigdy 2+ jednocześnie — więc nie wyłapała tego. Sugerowana poprawka: metoda największej reszty (Hamilton) zamiast "ostatni klucz dostaje resztę" — floor dla wszystkich, potem dosypać brakujące jednostki kluczom z największą częścią ułamkową, gwarantując sumę dokładnie `remaining` bez ujemnych wartości.

2. [CONFIRMED, węższa ale realna ścieżka dotarcia] Gdy `previousOwnerReligion` jest `null`/falsy ORAZ `counts` przed podbojem nie zawiera żadnej religii poza (ewentualnie) religią nowego właściciela — gałąź `else if (previousOwnerReligion && remaining > 0)` (linia 814) nie wykonuje się (bo `previousOwnerReligion` jest fałszywe), więc `remaining` NIE jest przypisywane NIKOMU i po prostu znika z `counts`. Reprodukcja: `before={counts:{}}`, `population=100`, `previousOwnerReligion=null`, różny okrąg → `after={counts:{rzym_bogowie:50}}`, suma=**50** zamiast **100** — I co gorsza, ponieważ udziały religijne (`religionOwnShare`, `dominantReligion`, `religionHappiness`) liczą się z sumy faktycznie obecnej w `counts` (nie z parametru `population`), zdobywca wychodzi z tego z **100% dominacją** (50/50) zamiast zamierzonych **50%** (50/100) — błąd nie tylko księgowy, ale realnie zniekształca wynik "kto dominuje" bezpośrednio po podboju. Odbiega to od zachowania `onCityCapturedCulture`, gdzie brak danych (`ownCultureShare ?? kulturaOwnShare ?? 1`) ma bezpieczny fallback (zakłada 100% starego właściciela, inwersja daje 0 nowemu — konserwatywne, nie gubi populacji); religijny odpowiednik takiego zabezpieczenia nie ma. Ścieżka dotarcia: `previousOwnerReligion` wychodzi `null`, gdy stary właściciel to `ownerId` bez wpisu w `aiOwnerCivMap` — z komentarza w `main.ts` (linia ~7932) wynika, że tylko barbarzyńcy nie mają tam wpisu; blok wywołujący `onCityCapturedReligion` jest bezwarunkowo pomijany TYLKO gdy `isBarbarian(atkOwner)` (zdobywca), NIE gdy `isBarbarian(oldOwner)` (stary właściciel) — czyli scenariusz "gracz odbija miasto trzymane wcześniej przez barbarzyńców" prowadzi do `previousOwnerReligion=null`. W praktyce `cityRelig` takiego miasta zwykle NIE jest pustym `{counts:{}}` (barbarzyńskie zawłaszczenie zamraża, nie czyści, poprzedni stan), więc pełny scenariusz-z-utratą-populacji wymaga dodatkowo, by miasto nigdy wcześniej nie miało zainicjalizowanego `ReligionState` (np. świeżo założone, właściciel bez religii w danych) — węższe, ale wciąż osiągalne bez anomalii danych. Sugerowana poprawka: jawna gałąź dla `!previousOwnerReligion && othersTotal<=0` zamiast cichego porzucenia `remaining` (np. traktować ten przypadek jak SAME-okrąg — brak realnych danych do inwersji, więc `defaultCityReligionState`).
