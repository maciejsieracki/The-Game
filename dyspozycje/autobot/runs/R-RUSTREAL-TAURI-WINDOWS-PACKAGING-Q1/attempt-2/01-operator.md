STATUS: INFRA
DOMAIN: INFRA
TEMAT: R-RUSTREAL-TAURI-WINDOWS-PACKAGING-Q1
GOAL: Kontynuować przygotowanie powtarzalnej paczki Tauri Windows x64 MSI/EXE bez wykonywania push/merge/deploy.
ZMIANY/COMMIT: HEAD 581bc93b78cc0e58c63b0e11a7fd11963501c5b8; branch hermes/R-RUSTREAL-TAURI-WINDOWS-PACKAGING-Q1-20260919; base origin/autobot/real24-staging 3cd225a8d48db84ee0a6ef5cb10cacaf4f1408d7. Zachowane źródła z allowlisty: .github/workflows/tauri-windows.yml oraz src-tauri/**; brak nowych zmian produkcyjnych w tej recovery. `src-tauri/src/lib.rs` pozostaje tracked i bez zmian względem bazy. `src-tauri/Cargo.lock` zachowany, bo workflow buduje z --locked. Wygenerowane src-tauri/gen, targety i nieallowlistowany rust-port/engine/Cargo.lock usunięte.
TESTY: JSON tauri.conf/capability PASS; frontend node --check PASS; YAML/static workflow contract PASS (windows-2022, x86_64-pc-windows-msvc, pinned tauri-cli 2.6.0, cargo tauri build --bundles msi,nsis --ci, exact MSI/NSIS upload paths, if-no-files-found=error); cargo metadata --manifest-path src-tauri/Cargo.toml --locked --no-deps PASS; tauri info PASS (Tauri 2.11.5, tauri-build 2.6.3); rustfmt PASS; Linux `tauri build --no-bundle --ci` PASS; Linux `tauri build --bundles deb --ci` PASS and icon PNG/ICO validation PASS. Engine `cargo test --all-targets --offline` PASS. `node tests/bridge_contract.cjs` PASS: 5 bridge tests, 1 Tauri mock IPC test, registration/event mutation controls PASS. Cross-target `cargo check --locked --target x86_64-pc-windows-msvc` reached civ-desktop but stopped in tauri-winres because host `llvm-rc` is unavailable; offline retry also lacked cached jiff-tzdb.
BLOKADY: Linux Tauri CLI rejects `--bundles msi,nsis` (only deb/rpm/appimage are valid on this host), so no local MSI/EXE exists. `cargo tauri` is not installed locally; the available npm CLI is 2.11.4 and was used only for local Linux validation. The packaging workflow is still untracked/local and was not pushed, so no GitHub Actions Tauri packaging run or Windows installer artifact is directly evidenced. Existing run 35457091894 is Rust engine parity, not Tauri packaging; its Windows success cannot close this gate.
RUNDY: 1/5; recovery attempt windows-x64-msi-exe-20260919-recovery2 after prior run 807 pid-not-alive crash.
NASTĘPNY KROK: INFRA/Windows runner gate: after the allowlisted files are integrated onto an authorized remote ref, run .github/workflows/tauri-windows.yml on windows-2022 and read back artifact `civ-the-game-windows-x64-installers` containing `src-tauri/target/release/bundle/msi/*.msi` and `src-tauri/target/release/bundle/nsis/*.exe`; then route to independent Evaluator.
DEPLOY/PUSH: NIE WYKONANO
