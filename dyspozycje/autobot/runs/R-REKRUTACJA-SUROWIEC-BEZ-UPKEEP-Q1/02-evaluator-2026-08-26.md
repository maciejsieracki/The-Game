# 02 — EVALUATOR (runda 1, 2026-08-26)

STATUS: PASS-WITH-NOTES
DOMAIN: GAME
TEMAT: R-REKRUTACJA-SUROWIEC-BEZ-UPKEEP-Q1
MODEL+EFFORT: Opus 5, effort high
GOAL: zgodny z `00-dispatch.md` — rekrutacja sprawdza wyłącznie `unitStockCost`; utrzymanie pobierane w następnej turze. Parytet gracz/AI/MP.

## ZMIANY/COMMIT
Weryfikowany HEAD `6ac34853`, merge-base `8b6a64d0`. Worktree Evaluatora `/home/user/wt-ev-rekrutacja` (detached), baza porównawcza `/home/user/wt-ev-rekrutacja-base` (`8b6a64d0`).
Allowlista **dotrzymana**: 10 plików, wszystkie w zakresie. `git diff origin/main..HEAD -- gra/src/main.ts gra/src/ui/buildModeHud.ts gra/data dyspozycje/WERSJE.md gra-robocza` → **pusto** (brak kolizji z `R-PRACA-PANEL-BUDOWY-WLASCIWA-WARSTWA-Q1`). Brak sekretów. Drzewo czyste po moich mutacjach.

## TESTY — wszystkie uruchomione moją ręką

**Trzy pre-istniejące czerwone (4 liczby, baza `8b6a64d0` → gałąź):**
`unit-stock-cost-test` **41/17 → 41/17** · `unit-resource-upkeep-test` **3/4 → 3/4** — bez pogorszenia, nie naprawione.
`ai-recruit-upkeep-gate-test` **18/9 → 23/0**, ale plik przepisany przez Operatora (pinował wycofaną decyzję) — **to nie jest naprawa driftu**.

**Zielone (moje uruchomienia):** `ai-prod-fallback` 17/0 (baza też 17/0) · `upkeep` 73/0 (baza 73/0) · `ai-mp-rekrutacja-build-gate` 21/0 · logic 213/213 · tech-tree 19/0 · research 33/33 · unit-replace 13/13 · combat 6/6 · `tsc --noEmit` **exit 0** · `vite build --outDir /tmp/civ-dist-ev` **OK** (index.html 37 475 001 B).
**Nowe:** `recruitment-no-upkeep-gate-test` 36/0 · `recruit-card-stock-chip-real-render-test` 15/0 w żywym Chromium.

**Scenariusz właściciela — mój pomiar (nie z raportu):** `canAffordUnitRecruitStock({drewno:57}, Wojownik)` = **true**, `{drewno:49}` = **false**, `{drewno:50}` = true, `pickUnitRecruitHint(57)` = `null`. Ścieżka faktycznie wołana przez `main.ts` (alias `canAffordUnitRecruitFull`): 57 → **true**, 49 → **false**, `alias === canAffordUnitRecruitStock` → true. Arność bramki = 2 (brak `ownerId`). Na bazie te same wywołania: 57 → false, hint „Za mało surowca (rekrutacja + utrzymanie 1 tura)". Usunięte `unitRecruitFullStockCost` / `canAffordUnitRecruitUpkeepReserve` / `UNIT_RECRUIT_FULL_HINT` → `undefined`; `unitRecruitUpkeepReserve` zostaje — potwierdziłem czytaniem `ai.ts:1885-1901`, że zbiera wyłącznie **klucze surowców** do priorytetu ulepszeń, nie dopuszcza rekrutacji.

## NAJWAŻNIEJSZE PYTANIE RUNDY — czy pobór utrzymania nie został wyłączony

Zmierzone **inaczej niż przez Operatora**: pętla **5 tur × 3 ownerów (0 / 3 / 7)**, prawdziwy `advanceCityEconomy` + `deductBuildingStockCostAcrossCities` + `missingStockFor`, ślad zrzucony do JSON i porównany 1:1 baza vs gałąź.
Harness i ślady: `dowody-ewaluatora-2026-08-26/{ev-upkeep-drain.cjs, drain-base.json, drain-branch.json, drain-base-vs-branch.diff}`.

Wynik (identyczny dla ownera 0, 3 i 7 — **parytet zmierzony, nie zadeklarowany**): rekrutacja 57 − 50 = 7 → tura 1: naliczone `{drewno:10}`, **niedobór `{drewno:3}`**, pula 7 → **0**; tury 2–5: naliczone `{drewno:10}`, **niedobór `{drewno:10}` co turę**, pula 0 (podłoga, bez ujemnej). Kontrola przeciwna bez jednostki: naliczone `{}`.
**Diff baza↔gałąź: 6 linii, wyłącznie werdykty bramki rekrutacji (`recruitAllowed`, `gate57`, `gate50`, `hint57`). Każda liczba poboru i niedoboru przez 5 tur jest bajt w bajt identyczna.** Konsekwencja niedoboru odpala się w każdej turze (`missingStockFor` niepusty → ostrzeżenie w `main.ts:26113-26120`).

## Dowody nietautologiczności — moje mutacje

- **M1** (bramka znów dolicza `unitRecruitUpkeepReserve`): `recruitment-no-upkeep` 36/0 → **28/8**, `ai-recruit-upkeep-gate` 23/0 → **21/2**, Chromium 15/0 → **14/1**.
- **M4** (`totalUnitResourceUpkeep` → `{}`): `recruitment-no-upkeep` 36/0 → **32/4**, a **mój** ślad wielotury staje się płaski (7 → 7, naliczone `{}`, niedobór `{}`) — czyli mój pomiar realnie wykrywa wyłączenie poboru, nie potwierdza sam siebie.
- Obie mutacje cofnięte, `git status` pusty. Test Chromium **nie** jest regexem po własnym źródle: `cityPanel.ts` czytany tylko po to, by wstrzyknąć prawdziwe reguły CSS; asercje idą z `getComputedStyle` w przeglądarce.

## NOTY (nie blokują)

1. `recruit-card-stock-chip-real-render-test` **odtwarza** okablowanie `appendUnitRecruitCompactRow`, zamiast je wołać. Równoważne z konstrukcji (`cityPanel.ts:7788` liczy ten sam `missingStockFor(pool, unitStockCost(udef))`, chip idzie przez ten sam `isUnitRecruitStockChipMissing`), ale funkcja produkcyjna nie jest renderowana wprost.
2. `ai-prod-fallback-test.cjs:271` nadal zielony (pula 5 < 50), lecz jego komentarz „12 brąz: 10 rekrutacja + 2 rezerwa" opisuje **wycofaną** decyzję — martwy komentarz do osobnego tematu.
3. Nagłówek karty na obu zrzutach brzmi „Miecznik · Kamień" przy definicji Wojownika — etykietowanie `buildUnitRecruitCard`, pre-istniejące i poza tematem; chipy (50 Drewno, −10 Drewno/t) są Wojownika z `units.json`.
4. Dług: 5 wywołań `@deprecated` aliasu w `main.ts` (rename poza allowlistą) oraz drift ×1/×5 w dwóch bramkach.

## BLOKADY
Brak.

RUNDY: 1/5
NASTĘPNY KROK: Final Control (worktree `/home/user/wt-ev-rekrutacja` zostawiony; baza porównawcza `/home/user/wt-ev-rekrutacja-base`).
DEPLOY/PUSH: NIE WYKONANO (wypchnięta wyłącznie gałąź tematu).
