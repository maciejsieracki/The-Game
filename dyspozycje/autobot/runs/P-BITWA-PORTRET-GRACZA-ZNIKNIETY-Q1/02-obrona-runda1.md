STATUS: DECISION_REQUIRED
DOMAIN: GAME
TEMAT: P-BITWA-PORTRET-GRACZA-ZNIKNIETY-Q1
GOAL: naprawić regres portretu gracza na ekranie „Wynik bitwy"
ZMIANY-COMMIT: brak zmian w `gra/` (regres nadal niereprodukowany dla pokrytych kierunków); ten plik obrony, commit na `autobot/P-BITWA-PORTRET-GRACZA-ZNIKNIETY-Q1`
TESTY: bez zmian względem rundy 1 — tsc 0 błędów; 5 bramek referencyjnych zielone (213/213, 19/19, 33/33, 13/13, 6/6); `r-bitwa-etykieta-tozsamosc-strony-live-atak-test.cjs` 21/21 PASS (baseline, bez mutacji)
RUNDY: 1/5

OBRONA:

1 -> PRZYJMUJE. Zweryfikowałem niezależnie: `grep -n "const modes" gra/tools/r-bitwa-etykieta-tozsamosc-strony-live-atak-test.cjs` → `modes = ['walka', 'mapa']` — oba scenariusze mają gracza jako stronę INICJUJĄCĄ atak (call site'y (a) i (c) w opisie zarzutu). Scenariusz (b) — AI/barbarzyńca atakuje gracza, `finishIncomingBattleUi` / `launchIncomingMapFieldBattle` (main.ts ~25886-26073, jedyne wywołanie `showPreBattle` z `auto: true`, komentarz w kodzie: "JEDYNE automatyczne (nie-gracz-inicjowane) wywołanie showPreBattle w całym kodzie") — rzeczywiście nie ma żadnego żywego pokrycia klikowego, tylko czytanie kodu. Sprawdziłem: DOWOD-PRZYCZYNY z rundy 1 opierał się dla tego call site'u wyłącznie na tym, że `BattleScene({...attackerCivIconId: pbInfo.atakujacy.civId, ...})` jest zbudowany strukturalnie tak samo jak w pozostałych dwóch call site'ach (potwierdzone ponownie w tej rundzie, main.ts ~26088-26120) — to redukuje ryzyko regresu, ale nie jest żywym dowodem i REGUŁA PRZECIW SAMOOSZUKIWANIU słusznie tego zabrania. Zgoda z Evaluatorem: to przeoczenie wymogu dispatchu (pkt 4), nie świadoma decyzja, i nie było ujawnione w BLOKADY — poprawka poniżej.

Ustalenie dodatkowe (dlaczego NIE naprawiam tego w tej samej rundzie z żywym dowodem): scenariusz (b) uruchamia się WYŁĄCZNIE z fazy AI/barbarzyńców na końcu tury (`launchIncomingMapFieldBattle` wołane z main.ts ~32628/33295, wewnątrz logiki decyzyjnej AI), nie z kliku gracza w mapę — w przeciwieństwie do (a)/(c) nie da się tego wywołać deterministycznie samym scenariuszem klik-w-canvas na istniejącym sandboxie `?playtest=walka`/`?playtest=mapa`. Sprawdziłem `grep -n "doStartPlaytest" main.ts` — nie istnieje żaden playtest-sandbox, który deterministycznie wymusza atak AI na gracza (wszystkie 3 istniejące sandboxy budują świat, ale nie sterują decyzją AI o ataku). Zbudowanie takiego dowodu wymaga albo (i) nowego deterministycznego sandboksu wymuszającego AI-agresję wobec jednostki gracza, albo (ii) analizy logiki decyzyjnej AI na tyle, by dobrać seed/pozycję dającą pewny atak — obie ścieżki to nowa infrastruktura testowa, nie „doklejenie trzeciego mode" jak sugeruje NASTĘPNY KROK Evaluatora (sam kod pokazuje, że to nie jest tylko dodanie `mode` do istniejącej pętli klik-w-canvas, tylko osobny mechanizm wyzwalania). Robienie tego pospiesznie w resztę tej rundy ryzykowałoby dokładnie to, przed czym ostrzega REGUŁA PRZECIW SAMOOSZUKIWANIU — słaby/niepewny dowód podpisany jako mocny.

Rozwiązanie: ujawniam to teraz jawnie jako nową BLOKADĘ (poniżej) zamiast domykać temat. Runda 2 dostarczy albo żywy dowód dla (b), albo, jeśli budowa deterministycznego sandboksu okaże się nieproporcjonalna do tego tematu, ECHO właściciela z jawnym opisem tej luki (analogicznie do luki miasto-państwo/barbarzyńca, którą właściciel już raz rozstrzygnął dla `9479ccc4`).

2 -> PRZYJMUJE. Zweryfikowałem bezpośrednio oba pliki: `05-pasek-naglowka.png` pokazuje nagłówek „Stan oddziałów" / STATUS „Bitwa w toku" (bitwa jeszcze trwa, przycisk „ZAMKNIJ") — to NIE jest ekran „Wynik bitwy". `06-werdykt.png` pokazuje nagłówek „Wynik bitwy" / WERDYKT „Rzymianie wygrywa" (przycisk „KONTYNUUJ") — to jest właściwy dowód na ekran „Wynik bitwy", i on rzeczywiście pokazuje realne portrety władców po obu stronach (Rzymianie i Grecy), nie sylwetki. Odsyłacz w raporcie rundy 1 był błędny — poprawiony cytat poniżej.

DOWOD-NAPRAWY-CHROMIUM (poprawiony): zrzut `/tmp/live-atak-2lMzqV/06-werdykt.png` — ekran „Wynik bitwy", oba medaliony (Rzymianie/Grecy) pokazują realny portret władcy (`<img>`), nie generyczną sylwetkę PB_SVG.commander. Zrzut `05-pasek-naglowka.png` z tego samego katalogu dokumentuje wyłącznie stan „Bitwa w toku" (poprzedzający wynik), zostawiam go jako dowód uzupełniający (medaliony obecne też w trakcie bitwy), ale nie jako dowód na ekran „Wynik bitwy".

NOWA BLOKADA (ujawniona w tej rundzie, nie w rundzie 1): scenariusz (b) — AI/barbarzyńca atakuje gracza, gracz jako obrońca (`finishIncomingBattleUi`, main.ts ~25886-26210) — bez żywego pokrycia Chromium w żadnej rundzie tego tematu. Kod strukturalnie identyczny z pokrytymi call site'ami (zweryfikowane ponownie), ale to czytanie kodu, nie żywy dowód. Wymaga nowej infrastruktury testowej (brak istniejącego deterministycznego sandboksu wymuszającego atak AI), więc nie da się bezpiecznie dostarczyć w pozostałym czasie tej rundy bez ryzyka słabego dowodu.

NASTĘPNY KROK: Runda 2 — zbudować żywy dowód Chromium dla scenariusza „AI atakuje gracza" (nowy sandbox lub dobrany seed/pozycja wymuszająca decyzję AI o ataku na jednostkę gracza) ALBO, jeśli koszt budowy takiego sandboksu okaże się nieproporcjonalny względem tego tematu, ECHO właściciela łączące obie ujawnione luki (miasto-państwo/barbarzyniec z rundy 1 + kierunek AI-atakuje-gracza z tej obrony) w jedno pytanie o zakres regresu.

DEPLOY/PUSH: NIE WYKONANO
